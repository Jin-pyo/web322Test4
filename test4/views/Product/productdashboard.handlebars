<h2>Product Listing</h2>

    <section class="products-contatiner">
        {{#each lists}}
        <div>
            {{this.title}}
            &nbsp;&nbsp;
            <a href="/product/edit/{{this._id}}">Edit</a>&nbsp;&nbsp;

        </div>

        {{else}}
            No tasks in database
        {{/each}}

    </section>